#include <iostream>
#include <ctime>
using namespace std;
void shoot(bool &targetAlive, double accuracy);
int main()
{
    while(true)
    {double rate;
    int count;
    srand(time(0));
    float a = (static_cast<double>(rand() % 101)) / 100;
    cout <<a;}
}